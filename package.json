{
  "name": "lms-webapp",
  "version": "1.0.0",
  "description": "Learning Management System Web Application",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-deps": "cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["lms", "education", "react", "nodejs"],
  "author": "LMS Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
